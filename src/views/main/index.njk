{% extends "../layouts/main.njk" %}

{% block head %}
    <link rel="stylesheet" href="/styles/main/main.css">
    <link rel="stylesheet" href="/styles//main/index.css">

    <title>Foodfy</title>
{% endblock head %}

{% block content %}
    <div class="about">
        <div class="text">
            <h1>As melhores receitas</h1>
            <p>Aprenda a contruir os melhores pratos com receitas criadas por profissionais do mundo inteiro.</p>
        </div>

        <img src="/assets/chef.png" alt="Desenho de chef">
    </div>

    <div class="most-accessed">
        <h1>Mais acessadas</h1>

        <div class="cards">
            <div class="card">
                <img src="{{ recipes[0].image }}" alt="{{ recipes[0].title }}">
                <h2>{{ recipes[0].title }}</h2>
                <p>por {{ recipes[0].author }}</p>
            </div>

            <div class="card">
                <img src="{{ recipes[1].image }}" alt="{{ recipes[1].title }}">
                <h2>{{ recipes[1].title }}</h2>
                <p>por {{ recipes[1].author }}</p>
            </div>

            <div class="card">
                <img src="{{ recipes[2].image }}" alt="{{ recipes[2].title }}">
                <h2>{{ recipes[2].title }}</h2>
                <p>por {{ recipes[2].author }}</p>
            </div>

            <div class="card">
                <img src="{{ recipes[3].image }}" alt="{{ recipes[3].title }}">
                <h2>{{ recipes[3].title }}</h2>
                <p>por {{ recipes[3].title }}</p>
            </div>

            <div class="card">
                <img src="{{ recipes[4].image }}" alt="{{ recipes[4].title }}">
                <h2>{{ recipes[4].title }}</h2>
                <p>por {{ recipes[4].title }}</p>
            </div>

            <div class="card">
                <img src="{{ recipes[5].image }}" alt="{{ recipes[5].title }}">
                <h2>{{ recipes[5].title }}</h2>
                <p>por {{ recipes[5].author }}</p>
            </div>
        </div>
    </div>
{% endblock content %}